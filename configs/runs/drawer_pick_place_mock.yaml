extends:
  - ../base.yaml
  - ../profiles/smoke.yaml

name: drawer_pick_place_mock
task:
  file: ../../tasks/drawer_pick_place_v1/task.yaml

policy:
  name: scripted_drawer_pick_place
  action_type: semantic
  config:
    strategy: articulated_state_machine

environment:
  backend: mock_2d
  config:
    scene_file: scenes/mock/drawer_pick_place_v1/scene.yaml
