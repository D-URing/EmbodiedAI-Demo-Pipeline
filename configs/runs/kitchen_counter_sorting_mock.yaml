extends:
  - ../base.yaml
  - ../profiles/smoke.yaml

name: kitchen_counter_sorting_mock
task:
  file: ../../tasks/kitchen_counter_sorting_v1/task.yaml

policy:
  name: scripted_kitchen_counter_sorting
  action_type: semantic
  config:
    strategy: category_to_zone

environment:
  backend: mock_2d
  config:
    scene_file: scenes/mock/kitchen_counter_sorting_v1/scene.yaml
