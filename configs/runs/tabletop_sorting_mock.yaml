extends:
  - ../base.yaml
  - ../profiles/smoke.yaml

name: tabletop_sorting_mock
task:
  file: ../../tasks/tabletop_sorting_v1/task.yaml

policy:
  name: scripted_sorting
  action_type: semantic
  config:
    strategy: color_to_zone

environment:
  backend: mock_2d
  config:
    scene_file: scenes/mock/tabletop_sorting_v1/scene.yaml
